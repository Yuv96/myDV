# 开发约束

- 本地只进行源码编辑、接口联调和 Git 提交；禁止本地编译、构建、单元测试、回归测试、模拟器测试及测试性质的检查命令。
- 所有编译和测试必须通过本仓库 GitHub Actions 执行，可使用 `gh` 查看结果。
- 本地接口联调使用 agent-webview，Python 以 `-B` / `PYTHONDONTWRITEBYTECODE=1` 运行。
- 扫码凭证、控制器 Token、Cookie、原始网络响应只放在 Git 忽略的 `.local-debug/` 中，不提交、不上传 CI、不打印到终端。
- 扫码必须由用户在官方抖音页面和手机上完成；账号接口验证成功前不替换已保存凭证。
- 点赞、关注需要指定操作对象；好友分享需要明确的接收人和发送授权。接口 HTTP 200 不代表业务成功，未验证的流程必须如实标注。
- 本次联调经用户授权临时向本机 `/etc/hosts` 添加 `lf-cdn-tos.bytescm.com` 解析，标记为 `# myDV-temp-dns-20260926`。项目结束时必须删除带此标记的行并刷新 DNS 缓存，不删除其他 hosts 条目。
