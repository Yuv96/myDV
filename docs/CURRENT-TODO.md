# 本次需求进度

约束：本地只编辑源码、进行真实接口联调和 Git 提交；所有编译、测试均在 GitHub Actions。两位 GPT-6 high 智能体分别负责评论区和右上角胶囊，主智能体负责扫码接口及整合。

- [ ] 扫码与互动接口
  - [x] 创建本地专属目录 `myDV/`，启动 agent-webview。
  - [x] 定位验证码脚本 DNS 拦截，临时 hosts 解析后脚本 HTTP 200。
  - [x] 官方取码接口本次返回真实二维码。
  - [x] 官方网页扫码后读取 HttpOnly Cookie，独立 HTTP 账号和好友列表验证成功。
  - [x] 官方网页点赞与取消点赞均返回 status_code=0，独立读取确认回到原始未点赞状态。
  - [x] 核对关注请求及最终状态：官方网页请求返回 status_code=8（用户未登录），未关注状态保持原样，未误报成功。
  - [ ] Android 独立 HTTP 扫码轮询仍返回 4031；需要完整浏览器身份/签名链路，不能声称电视端扫码已修复。
  - [ ] 关注写入尚未成功；好友列表已读取，但未指定接收人，不发送真实好友视频。
- [x] 评论区纯阅读改造
  - [x] 评论项无焦点、不可选中，仅上下滚动；去除底部按钮和多余文案。
  - [x] 触底自动分页，内存条数限制不终止继续加载。
  - [x] 昵称和点赞数为正文半号，头像固定大小靠每条评论右侧。
  - [x] 去除示例评论的编号前缀，正文直接显示评论内容。
  - [x] GitHub API 21 回归通过，包括自动分页、无焦点、快速长按滚动和生命周期。
- [x] 时间、通知数和登录状态合并到右上角胶囊
  - [x] 修改布局与必要回归。
  - [x] 移除居中的登录/评论权限提示；已登录显示通知数，未登录或权限失效显示“未登录”。
  - [x] 分隔线单独淡化，两侧增加对称间距；没有已知数量时显示 `--`，断网不误报退出登录。
  - [x] GitHub 构建与回归通过，包括凭证变化、通知数、账号切换和胶囊样式。

## 验证与收尾

- Android 源码提交 `41adbb8`：[GitHub 编译、API 21 回归及正式 APK 检查通过](https://github.com/Yuv96/myDV/actions/runs/36215763358)，同页可下载 `android5-player-apk` 和截图日志。
- 调试工具：[Python 3.10/3.12 用例通过](https://github.com/Yuv96/myDV/actions/runs/36215263328)。
- 实际账号最终独立读取：已登录，点赞 `0`、关注 `0`，与操作前一致；未向好友发送消息。
- 本地 agent-webview 调试窗口与控制器已关闭；凭证仅保留于被忽略的 `.local-debug/`。
- 临时 hosts 删除命令已发起，等待 macOS 管理员验证；完成后只移除带 `# myDV-temp-dns-20260926` 的行并刷新 DNS 缓存。
